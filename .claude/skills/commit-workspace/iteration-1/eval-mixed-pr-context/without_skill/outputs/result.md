# Questions Asked
None

# Commit Message
feat(display): add mixed DPI detection and EDID registry fallback

# Reasoning
The changes introduce two new methods for display detection — `detect_mixed_dpi()` and `_get_monitor_edid()` with a registry fallback — which constitute a new feature addition to the display_setter module. The `feat` type and `display` scope accurately reflect what changed.
